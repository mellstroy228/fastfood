function addToCart(name, price) {
    // Сохраняем товар в базе данных (например, отправляем запрос на сервер)

    // Перенаправляем пользователя на страницу корзины
    window.location.href = 'cart.html';
}

